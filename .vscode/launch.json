{
    "configurations": [
    
        {
            "name": "C/C++: clang Aktive Datei erstellen und debuggen",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang++ Aktive Datei kompilieren"
        }
    ],
    "version": "2.0.0"
}